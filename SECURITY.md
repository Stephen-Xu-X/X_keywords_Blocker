# Security

Do not paste cookies, `auth_token`, `ct0`, or exported request headers into issues or pull requests. The userscript reads authentication state only from the active X tab and stores the transient transaction id in tab-scoped `sessionStorage`.

Report suspected security issues privately to the repository maintainers rather than publishing live credentials.
